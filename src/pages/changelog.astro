---
/**
 * /changelog/ — English route (ADR-0010). Page body lives in
 * `ChangelogPage`; this is a routing wrapper. Localized equivalents render
 * the same component from `/[locale]/changelog/`. Body stays EN in all
 * locales per ADR-0011.
 */
import ChangelogPage from '@/components/content/ChangelogPage.astro';
---

<ChangelogPage />
