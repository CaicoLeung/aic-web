---
/**
 * /resolve/ — English route (ADR-0010). The page body lives in
 * `ResolvePage`; this is a routing wrapper. Localized equivalents render
 * the same component from `/[locale]/resolve/`.
 */
import ResolvePage from '@/components/content/ResolvePage.astro';
---

<ResolvePage />
