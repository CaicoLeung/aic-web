---
/**
 * /[locale]/alternatives/ai-commit/ — localized migration page (ADR-0010).
 */
import { isLocalePrefix, LOCALE_PREFIXES } from '@/i18n/config';
import AlternativePage from '@/components/content/AlternativePage.astro';
import { AI_COMMIT_COMPARISON } from '@/config/competitors';

export function getStaticPaths() {
  return LOCALE_PREFIXES.map((locale) => ({ params: { locale } }));
}

const { locale } = Astro.params;
if (!isLocalePrefix(locale)) {
  return new Response(null, { status: 404, statusText: 'Not Found' });
}
---

<AlternativePage id="ai-commit" comparison={AI_COMMIT_COMPARISON} />
