---
/**
 * /agents/ — English route (ADR-0010). The page body lives in
 * `AgentsPage`; this is a routing wrapper. Localized equivalents render
 * the same component from `/[locale]/agents/`.
 */
import AgentsPage from '@/components/content/AgentsPage.astro';
---

<AgentsPage />
