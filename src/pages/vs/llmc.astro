---
/**
 * /vs/llmc/ — English route (ADR-0010). Page body lives in
 * `VsCompetitorPage`; this is a routing wrapper. Localized equivalents
 * render the same component from `/[locale]/vs/llmc/`.
 */
import VsCompetitorPage from '@/components/content/VsCompetitorPage.astro';
import { LLMC_COMPARISON } from '@/config/competitors';
---

<VsCompetitorPage id="llmc" comparison={LLMC_COMPARISON} />
