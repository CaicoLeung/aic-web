---
/**
 * ScrollDemo — V2 scroll-driven product demo.
 *
 * A 300vh section. Heading scrolls normally, then a terminal + git-graph
 * pair pins and cross-fades through the aic workflow:
 *   0 → Stage ($ aic detects changes)
 *   1 → Review (batched commit plan)
 *   2 → Ship (commits landed on graph)
 *
 * Frames are position: absolute (overlap, no layout shift). A stage badge
 * in the terminal chrome tracks the current step. A mini git-graph grows
 * nodes in sync with the frames.
 *
 * No-JS / reduced-motion: the final frame (Ship) is visible by default.
 * V2-only: mounted only on /v2 routes.
 */
---

<section class="scroll-demo" data-scroll-demo>
  <!-- Heading — scrolls normally before the pin -->
  <div class="demo-heading container">
    <p class="demo-eyebrow">
      <span class="line" aria-hidden="true"></span>see it work
    </p>
    <h2 class="demo-h2">From 12 files<br />to 3 commits</h2>
  </div>

  <div class="demo-pin">
    <div class="demo-inner container">
      <!-- Left: persistent git-graph track -->
      <div class="demo-graph" aria-hidden="true">
        <div class="graph-origin" title="main"></div>
        <div class="graph-seg" data-seg="1">
          <span class="seg-line"></span>
          <span class="seg-node" data-node="1">
            <span class="node-label">feat</span>
          </span>
        </div>
        <div class="graph-seg" data-seg="2">
          <span class="seg-line"></span>
          <span class="seg-node" data-node="2">
            <span class="node-label">fix</span>
          </span>
        </div>
        <div class="graph-seg" data-seg="3">
          <span class="seg-line"></span>
          <span class="seg-node" data-node="3">
            <span class="node-label">chore</span>
          </span>
        </div>
      </div>

      <!-- Right: terminal frames -->
      <div class="demo-terminal">
        <div class="term-chrome">
          <div class="term-dots">
            <i></i><i></i><i></i>
          </div>
          <span class="term-title">aic — live demo</span>
          <span class="stage-badge" data-stage-badge>stage</span>
        </div>

        <div class="frame-stack">
          <!-- Frame 0: Stage -->
          <div class="demo-frame" data-frame="0">
            <pre
              class="term-body"><span class="prompt">$</span> <span class="cmd">aic</span>

<span class="muted">Scanning 12 changed files…</span>
<span class="cyan">3 logical commits detected</span>
<span class="muted">━ reviewing plan ━</span></pre>
          </div>

          <!-- Frame 1: Review -->
          <div class="demo-frame" data-frame="1">
            <pre
              class="term-body"><span class="label">commit 1</span>  <span class="amber">feat(auth): refresh token rotation</span>
           <span class="muted">auth/refresh.ts, auth/verify.ts</span>

<span class="label">commit 2</span>  <span class="amber">fix(ui): redirect on session expiry</span>
           <span class="muted">components/Redirect.tsx</span>

<span class="label">commit 3</span>  <span class="amber">chore: bump dependencies</span>
           <span class="muted">package.json, lock.json</span>

<span class="prompt">→</span> <span class="cmd">aic commit --all</span></pre>
          </div>

          <!-- Frame 2: Ship -->
          <div class="demo-frame" data-frame="2">
            <pre
              class="term-body"><span class="mint">✓ 3 commits created</span>

<span class="hash">abc1234</span>  <span class="muted">chore: bump dependencies</span>
<span class="hash">def5678</span>  <span class="muted">fix(ui): redirect on expiry</span>
<span class="hash">ghi9012</span>  <span class="amber">feat(auth): refresh token rotation</span>

<span class="cyan">12 files → 3 commits in 4.2s</span></pre>
          </div>
        </div>
      </div>
    </div>

    <!-- Scroll hint -->
    <div class="demo-hint" aria-hidden="true">
      <span class="hint-text">scroll</span>
      <span class="hint-arrow">↓</span>
    </div>
  </div>
</section>

<style>
  .scroll-demo {
    position: relative;
    background: var(--color-base-deep);
  }

  /* ── Heading (scrolls normally before pin) ────────────────────────── */
  .demo-heading {
    padding-top: clamp(3rem, 1rem + 6vw, 5rem);
    padding-bottom: 0;
    max-width: 48rem;
  }
  .demo-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-cyan);
    margin: 0 0 0.75rem;
  }
  .demo-eyebrow .line {
    width: 2rem;
    height: 2px;
    background: var(--color-cyan);
    border-radius: 1px;
  }
  .demo-h2 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 1rem + 3vw, 3.2rem);
    font-weight: 700;
    line-height: 1.05;
    margin: 0;
    color: var(--color-ink);
  }

  /* ── Pinned area ──────────────────────────────────────────────────── */
  .demo-pin {
    position: sticky;
    top: 0;
    height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .demo-inner {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: clamp(2rem, 1rem + 4vw, 5rem);
    align-items: center;
    width: 100%;
  }

  /* ── Git graph track ─────────────────────────────────────────────── */
  .demo-graph {
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
    gap: 0;
    min-height: 280px;
  }

  .graph-origin {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    border: 2px solid var(--color-ink-dim);
    background: var(--color-base);
    flex-shrink: 0;
  }

  .graph-seg {
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 0;
    transform: translateY(0.5rem);
    transition:
      opacity 0.4s ease,
      transform 0.4s ease;
  }
  .graph-seg.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .seg-line {
    width: 2px;
    height: 48px;
    background: linear-gradient(
      to bottom,
      color-mix(in oklch, var(--color-amber) 50%, transparent),
      var(--color-amber)
    );
  }

  .seg-node {
    position: relative;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--color-amber);
    box-shadow: 0 0 16px -2px color-mix(in oklch, var(--color-amber) 60%, transparent);
    flex-shrink: 0;
  }

  .node-label {
    position: absolute;
    left: 28px;
    top: 50%;
    transform: translateY(-50%);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-ink-muted);
    white-space: nowrap;
    letter-spacing: 0.05em;
  }

  /* ── Terminal ────────────────────────────────────────────────────── */
  .demo-terminal {
    position: relative;
    border: 1px solid var(--color-line);
    border-radius: 12px;
    overflow: hidden;
    background: var(--color-surface);
    box-shadow:
      inset 0 1px 0 0 rgba(255, 255, 255, 0.03),
      0 20px 60px -20px rgba(0, 0, 0, 0.5),
      0 0 80px -40px var(--color-cyan-soft, transparent);
  }

  .term-chrome {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1rem;
    border-bottom: 1px solid var(--color-line);
    background: rgba(0, 0, 0, 0.2);
  }

  .term-dots {
    display: flex;
    gap: 6px;
  }
  .term-dots i {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    display: block;
  }
  .term-dots i:nth-child(1) {
    background: #ff5f57;
  }
  .term-dots i:nth-child(2) {
    background: #febc2e;
  }
  .term-dots i:nth-child(3) {
    background: #28c840;
  }

  .term-title {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--color-ink-dim);
    margin-left: 0.5rem;
  }

  /* Stage badge — shows current workflow step */
  .stage-badge {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-cyan);
    padding: 0.2rem 0.6rem;
    border: 1px solid color-mix(in oklch, var(--color-cyan) 30%, var(--color-line));
    border-radius: 99px;
    background: color-mix(in oklch, var(--color-cyan) 8%, transparent);
    transition:
      color 0.3s ease,
      border-color 0.3s ease,
      background 0.3s ease;
  }
  .stage-badge[data-stage='1'] {
    color: var(--color-amber);
    border-color: color-mix(in oklch, var(--color-amber) 30%, var(--color-line));
    background: color-mix(in oklch, var(--color-amber) 8%, transparent);
  }
  .stage-badge[data-stage='2'] {
    color: var(--color-mint);
    border-color: color-mix(in oklch, var(--color-mint) 30%, var(--color-line));
    background: color-mix(in oklch, var(--color-mint) 8%, transparent);
  }

  /* ── Frame stack — absolute overlap, no layout shift ─────────────── */
  .frame-stack {
    position: relative;
    min-height: 280px;
  }

  .demo-frame {
    position: absolute;
    inset: 0;
    padding: 1.25rem 1.5rem;
    opacity: 0;
    transform: translateY(8px);
    transition:
      opacity 0.35s ease,
      transform 0.35s ease;
  }
  /* No-JS fallback: show final frame only */
  .demo-frame[data-frame='2'] {
    opacity: 1;
    transform: translateY(0);
  }
  /* When JS runs, it manages all state */
  .scroll-demo[data-js] .demo-frame {
    opacity: 0;
    transform: translateY(8px);
  }
  .scroll-demo[data-js] .demo-frame.is-active {
    opacity: 1;
    transform: translateY(0);
  }

  .term-body {
    margin: 0;
    font-family: var(--font-mono);
    font-size: 0.82rem;
    line-height: 1.65;
    white-space: pre-wrap;
    color: var(--color-ink);
  }

  .prompt {
    color: var(--color-cyan);
    font-weight: 600;
  }
  .cmd {
    color: var(--color-ink);
    font-weight: 600;
  }
  .muted {
    color: var(--color-ink-dim);
  }
  .cyan {
    color: var(--color-cyan);
  }
  .amber {
    color: var(--color-amber);
  }
  .mint {
    color: var(--color-mint);
    font-weight: 600;
  }
  .hash {
    color: var(--color-ink-muted);
  }
  .label {
    color: var(--color-cyan);
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-weight: 600;
  }

  /* ── Scroll hint ─────────────────────────────────────────────────── */
  .demo-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    opacity: 0.5;
    transition: opacity 0.3s ease;
  }
  .demo-hint.is-hidden {
    opacity: 0;
  }
  .hint-text {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-ink-dim);
  }
  .hint-arrow {
    color: var(--color-ink-dim);
    font-size: 1rem;
    animation: hint-bounce 1.5s ease-in-out infinite;
  }
  @keyframes hint-bounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(4px);
    }
  }

  /* ── Mobile ──────────────────────────────────────────────────────── */
  @media (max-width: 768px) {
    .demo-inner {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
    .demo-graph {
      flex-direction: row;
      min-height: auto;
      justify-content: center;
    }
    .graph-seg {
      flex-direction: row;
    }
    .seg-line {
      width: 32px;
      height: 2px;
    }
    .node-label {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hint-arrow {
      animation: none;
    }
    .demo-frame {
      transform: none !important;
    }
  }
</style>

<script>
  const section = document.querySelector<HTMLElement>('[data-scroll-demo]');
  if (!section) {
    // V1 page — no scroll demo
  } else {
    const frames = section.querySelectorAll<HTMLElement>('.demo-frame');
    const segs = section.querySelectorAll<HTMLElement>('.graph-seg');
    const hint = section.querySelector<HTMLElement>('.demo-hint');
    const badge = section.querySelector<HTMLElement>('[data-stage-badge]');
    const totalFrames = frames.length;
    const stageNames = ['stage', 'review', 'ship'];

    // Mark JS active — takes over opacity management
    section.setAttribute('data-js', '');

    let ticking = false;

    function update() {
      ticking = false;
      const rect = section.getBoundingClientRect();
      const scrollable = rect.height - window.innerHeight;
      const raw = -rect.top / scrollable;
      const progress = Math.max(0, Math.min(1, raw));

      // Map progress to frame index
      const frame = Math.min(totalFrames - 1, Math.floor(progress * totalFrames));

      frames.forEach((f, i) => f.classList.toggle('is-active', i === frame));

      // Graph nodes: grow with frames, show all on final (ship) frame
      const visibleCount = frame === totalFrames - 1 ? segs.length : frame;
      segs.forEach((s, i) => s.classList.toggle('is-visible', i < visibleCount));

      // Stage badge
      if (badge) {
        badge.textContent = stageNames[frame];
        badge.setAttribute('data-stage', String(frame));
      }

      // Hide scroll hint after scrolling starts
      if (hint) hint.classList.toggle('is-hidden', progress > 0.05);
    }

    function onScroll() {
      if (!ticking) {
        requestAnimationFrame(update);
        ticking = true;
      }
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', update);
    update();
  }
</script>
